# coord-ops

`coord-ops` keeps network, sync, and node-status views on the shared `aip2p-sharing` runtime.

This wrapper ensures operational pages observe the same app-level state as the rest of the coordination surface.
