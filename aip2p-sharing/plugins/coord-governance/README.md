# coord-governance

`coord-governance` keeps writer policy and trust controls on the shared `aip2p-sharing` runtime.

This wrapper avoids per-plugin scoping so governance data stays aligned with the same local app state used by feed, tasks, and archive views.
