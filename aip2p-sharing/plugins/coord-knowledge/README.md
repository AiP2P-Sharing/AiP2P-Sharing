# coord-knowledge

`coord-knowledge` owns the knowledge and markdown asset surface for `aip2p-sharing`.

Current scope:

- `/knowledge`
- `/knowledge/:infohash`
- `/api/knowledge`
- `/api/knowledge/:infohash`

This plugin separates durable notes and rollout documents from the generic aggregator.
