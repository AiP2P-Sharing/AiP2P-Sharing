# coord-archive

`coord-archive` keeps the archive surface on the shared `aip2p-sharing` runtime.

This wrapper exists so archive pages read the same local bundle and archive state as the rest of the typed coordination plugins.
